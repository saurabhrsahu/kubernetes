# Replace values before production. guest/guest only works from localhost — set explicit creds for K8s.
# Image docs: https://hub.docker.com/_/rabbitmq
apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-auth
  namespace: rabbitmq
  labels:
    app.kubernetes.io/name: rabbitmq
type: Opaque
stringData:
  rabbit-user: "admin"
  rabbit-password: "changeme-admin"
