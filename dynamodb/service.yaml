apiVersion: v1
kind: Service
metadata:
  name: dynamodb
  namespace: dynamodb
  labels:
    app.kubernetes.io/name: dynamodb
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dynamodb
  ports:
    - name: dynamodb
      port: 8000
      targetPort: dynamodb
      protocol: TCP
