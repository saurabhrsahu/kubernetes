apiVersion: v1
kind: Service
metadata:
  name: memcached
  namespace: memcached
  labels:
    app.kubernetes.io/name: memcached
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: memcached
  ports:
    - name: memcache
      port: 11211
      targetPort: memcache
      protocol: TCP
