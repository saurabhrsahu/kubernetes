# Replace values before production. For real clusters use Sealed Secrets, External Secrets, or a secret manager.
apiVersion: v1
kind: Secret
metadata:
  name: redis-auth
  namespace: redis
  labels:
    app.kubernetes.io/name: redis
type: Opaque
stringData:
  redis-password: "changeme-redis"
